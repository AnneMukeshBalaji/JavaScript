// ! Ternary operator 
// ? Syntax  ::::: condition ? statement1 : statement2
// * if condition is true statement1 will execute if not then statement2 will execute 

console.log(5 > 0 ? 'This statement is true' : 'This statement is false ')


//  ! type checking operators
/*
?| ------------ | --------------------------- | ------------------- |
?| Operator     | Description                 | Example             |
*| ------------ | --------------------------- | ------------------- |
*| `typeof`     | Returns the type of operand | `typeof "abc"`      |
*| ------------ | --------------------------- | ------------------- |
*| `instanceof` | Checks instance of class    | `x instanceof Date` |
*| ------------ | --------------------------- | ------------------- |
*/


/* 
?| -------- | ------------- | ----------- |
?| Operator | Description   | Example     |
?| -------- | ------------- | ----------- |
*| `+`      | Concatenation | `"a" + "b"` |
*| -------- | ------------- | ----------- |
*/

/* 
    !Comma Operator ::: 
        ?Evaluates multiple expressions and returns the last:
*/

